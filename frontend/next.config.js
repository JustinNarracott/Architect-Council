/** @type {import('next').NextConfig} */
const nextConfig = {
    // strict mode is enabled by default in next 13.4+ but good to be explicit as requested
    reactStrictMode: true,
};

module.exports = nextConfig;
